# 🚀 Инструкция по запуску микросервисов Audio Store

## Быстрый старт - все сервисы сразу

### 1. Запуск всех микросервисов одной командой
```bash
python start_services.py
```

Этот скрипт автоматически запустит:
- 📚 **Микросервис "Каталог"** на порту 8001
- 🛒 **Микросервис "Корзина"** на порту 8002  
- 📦 **Микросервис "Заказы"** на порту 8003

## Запуск отдельных микросервисов

### Микросервис "Заказы" (порт 8003)
```bash
# Способ 1: Рекомендуемый (из корневой директории)
python run_orders_service.py

# Способ 2: Через модуль
python -m services.orders.run_app

# Способ 3: Из директории сервиса
cd services/orders
python simple_run.py
```

### Микросервис "Корзина" (порт 8002)
```bash
cd services/cart
python run_app.py
```

### Микросервис "Каталог" (порт 8001)
```bash
cd services/catalog
python main.py
```

## Тестирование и демонстрация

### Демонстрация микросервиса "Заказы"
```bash
# Запуск демо из корневой директории
python demo_orders.py

# Запуск демо как модуль
python -m services.orders.demo
```

### Проверка работы сервисов
```bash
# Health check каждого сервиса
curl http://localhost:8001/health  # Каталог
curl http://localhost:8002/health  # Корзина
curl http://localhost:8003/health  # Заказы
```

## API документация

После запуска сервисов доступна интерактивная документация:

- 📚 **Каталог**: http://localhost:8001/docs
- 🛒 **Корзина**: http://localhost:8002/docs  
- 📦 **Заказы**: http://localhost:8003/docs

## Подготовка базы данных

Перед первым запуском создайте таблицы:

```bash
# Создание таблиц для заказов
python create_orders_tables.py

# Инициализация всей базы данных
python init_db.py
```

## Примеры использования API

### Создание заказа
```bash
curl -X POST "http://localhost:8003/api/v1/orders" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {"audiobook_id": 1, "quantity": 2},
      {"audiobook_id": 2, "quantity": 1}
    ]
  }'
```

### Расчет корзины
```bash
curl -X POST "http://localhost:8002/api/v1/cart/calculate" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {"audiobook_id": 1, "quantity": 2}
    ]
  }'
```

### Получение списка аудиокниг
```bash
curl "http://localhost:8001/api/v1/audiobooks"
```

## Архитектура взаимодействия

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Каталог   │    │   Корзина   │    │   Заказы    │
│  (8001)     │    │   (8002)    │    │   (8003)    │
└─────────────┘    └─────────────┘    └─────────────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │
                    Получение данных
                    о товарах для
                    валидации корзины
```

### Процесс создания заказа:
1. **Клиент** отправляет "сырую" корзину в **Заказы**
2. **Заказы** отправляет корзину на валидацию в **Корзина**  
3. **Корзина** запрашивает данные о товарах у **Каталога**
4. **Корзина** возвращает валидированную корзину с ценами
5. **Заказы** создает заказ в базе данных
6. **Заказы** возвращает информацию о созданном заказе

## Troubleshooting

### Ошибка "Сервис недоступен"
- Убедитесь, что все зависимые сервисы запущены
- Проверьте, что порты не заняты другими процессами

### Ошибка импорта модулей
- Запускайте скрипты из корневой директории проекта
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`

### Ошибка подключения к базе данных
- Запустите `python create_orders_tables.py` для создания таблиц
- Проверьте файл `database/connection.py`

## Остановка сервисов

Для остановки всех сервисов нажмите `Ctrl+C` в терминале со скриптом `start_services.py`.

Для остановки отдельных сервисов используйте `Ctrl+C` в соответствующих терминалах.

---

🎉 **Готово!** Теперь у вас работает полная микросервисная архитектура Audio Store! 